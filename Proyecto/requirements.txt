# Requisitos del proyecto FastAPI
# Serán utilizadas por el entorno virtual

# PASO A PASO DEL ENTORNO VIRTUAL
"""
1️⃣ Crear el entorno virtual (una sola vez):
   python -m venv venv
2️⃣ Activar el entorno virtual cada vez que quieras trabajar:
   Windows: venv\Scripts\activate
   Linux/macOS: source venv/bin/activate
3️⃣ Instalar las dependencias:
   pip install -r requirements.txt
4️⃣ Para actualizar dependencias después de instalar algo nuevo:
   pip freeze > requirements.txt
5️⃣ Para desactivar el entorno cuando termines:
   deactivate
6️⃣ Para levantar la API FastAPI mientras el entorno está activo:
   uvicorn main:app --reload
"""

# Framework web y servidor
fastapi==0.102.0           # Framework web
uvicorn[standard]==0.23.1  # Servidor ASGI para FastAPI

# Validación y manejo de datos
pydantic==2.7.0            # Modelos de datos y validación
typing-extensions==4.8.0   # Para soportar tipos modernos (List, Any, etc.)

# Consumo de APIs externas
requests==2.32.0            # Cliente HTTP síncrono
httpx==1.4.0                # Cliente HTTP asíncrono, útil para FastAPI

# Variables de entorno
python-dotenv==1.0.0        # Manejo de .env

# Pruebas unitarias
pytest==8.3.2               # Framework de testing

# Base de datos y persistencia
sqlalchemy==2.0.45          # ORM para bases de datos relacionales
databases==0.7.1            # Manejo async con SQLAlchemy y FastAPI

# Manipulación de datasets
pandas==2.1.0               # Lectura y manipulación de CSV/XLS externos

# Logging y monitoreo
loguru==0.7.0               # Logging simple y flexible
